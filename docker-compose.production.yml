services:
  gatewayapi:
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
    ports:
      - "80:8080"

  consul:
    # Consul might be managed externally in production, but we keep it for consistency
    ports:
      - "8502:8500"
